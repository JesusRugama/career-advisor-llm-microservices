import sys
import os

# Add src to path so we can import our modules
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))

# This file can be used for shared test fixtures in the future
# Currently, our tests use simple mocks and don't require complex fixtures
